[package]
name = "imap-cache-mime"
version = "0.1.0"
edition = "2024"

[dependencies]
imap-cache-core = { path = "../core" }
imap-cache-storage = { path = "../storage" }
hex = "0.4"
mailparse = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
