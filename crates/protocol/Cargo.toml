[package]
name = "imap-cache-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
imap-cache-core = { path = "../core" }
chrono = { version = "0.4", features = ["serde"] }
serde_json = "1"
thiserror = "2"
